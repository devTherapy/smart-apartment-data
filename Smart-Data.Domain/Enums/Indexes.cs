namespace Smart_Data.Domain.Enums
{
    //all indexes should be lowercase for elastic search indexing to work properly.
    public enum Indexes
    {
        managements,
        properties
    }

    public enum Aliases
    {
        entities
    }
}
